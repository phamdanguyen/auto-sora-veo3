---
description: Build the Uni-Video executable (One-click)
---
This workflow allows you to build the standalone executable for deployment.

1. Run the build script:
    // turbo
    python build_release.py

2. Verify the output:
    - Check that `release_pkg/Uni-Video/` exists.
    - Check that `release_pkg/KeyGen.exe` exists.
